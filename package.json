{
  "name": "snapshot",
  "version": "0.1.0",
  "description": "A tiny library for reconciling server provided data with local data changes that occur between initiating a save and receiving the updated data from the server. Designed to work with Redux and other functional, flux architectures.",
  "main": "index.js",
  "dependencies": {},
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "redux"
  ],
  "author": "mful (mattfulton.co)",
  "license": "MIT"
}
